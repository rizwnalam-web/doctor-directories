services:
  - type: web
    name: doctor-directories-api
    env: node
    buildCommand: npm install && npx prisma generate
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: doctor-directories-db
          property: connectionString
      - key: NODE_ENV
        value: production
