# ========================================
# Environment Variables Template
# ========================================
#
# For Development:
#   1. Copy .env.development to .env
#   2. Update values with your local settings
#
# For Production (Render):
#   1. Copy .env.production to .env
#   2. Update values with your production settings
#   3. Configure in Render dashboard
#
# ========================================

# Database
DATABASE_URL="postgresql://username:password@localhost:5432/doctor_directories?schema=public"

# JWT Secret (Generate a unique secret for production)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=5000
NODE_ENV="development"

# Email Configuration (for notifications)
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT=587
EMAIL_USER="your-email@gmail.com"
EMAIL_PASS="your-app-password"
EMAIL_FROM="Doctor Directories <noreply@doctordirectories.com>"

# Frontend URL
CLIENT_URL="http://localhost:5173"

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_PATH="./uploads"

# ========================================
# Production Deployment Notes:
# ========================================
#
# 1. Use strong, unique secrets in production
# 2. Configure email with app passwords (not regular passwords)
# 3. Update CLIENT_URL with your production domain
# 4. Database URL will be provided by your hosting platform
# 5. Never commit real credentials to version control
